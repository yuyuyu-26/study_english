<% @answers.each do |answer| %>
  <!--%= answer[:word].english %--!>
  <!--%= answer[:japanese] ? "正解" : "不正解" %>
  <!%= answer[:word].japanese %--!>
  <%= answer[:word] %>
  <%= answer[:japanese] ? "正解" : "不正解" %>
  <%= answer[:japan] %>
<% end %>
