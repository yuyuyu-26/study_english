<%= form_for(@word) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :english, placeholder: "単語" %>
    <%= f.text_area :japanese, placeholder: "日本語" %>
  </div>
  <%= f.submit "作成", class: "btn btn-primary" %>
<% end %>
